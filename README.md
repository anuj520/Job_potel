src/
 ├── axios/
 │    └── axios.js          # Base URL Configuration (Axios Instance)
 └── pages/                 # Main Pages Folder
      ├── auth/             # Auth module inside pages
      │    ├── Login.jsx    # Login Page (JWT Token Generation)
      │    └── Signup.jsx   # User Registration
      └── User.jsx          # Profile Dashboard (Files & Education Integration)